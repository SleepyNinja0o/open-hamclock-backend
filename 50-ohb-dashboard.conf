url.rewrite-if-not-file = (
 "^/$" => "/index.html",
 "^/ham/?$" => "/index.html",
 "^/ham/HamClock/?$" => "/index.html"
)

$HTTP["useragent"] =~ "curl|wget|httpie" {
 url.rewrite-once = (".*" => "/ascii.txt")
}
